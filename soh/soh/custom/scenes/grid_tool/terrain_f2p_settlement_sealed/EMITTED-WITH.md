# terrain_f2p_settlement_sealed - emitted with

**Generated by `tools/terrain/emit-scene.ts`. Do not edit; re-emitting overwrites it.**

```
npx tsx ../terrain/emit-scene.ts --merge --composite="Lumbridge Settlement X3@56,64" --name=terrain_f2p_settlement_sealed
```

Run from `tools/grid-scene-tool/`. Re-running that exact line reproduces this scene, provided the
field digest below still matches.

| | |
|---|---|
| field | `baked/field.json` |
| field sha256 (first 16) | `8c7021ad613b708a` |
| field name / grid | f2p-greybox, 128x128 cells, step 3, 40 units/tile |
| merge | on, tolerance 0 |
| T-junction stitching (#95) | on |
| wall height | 6 tiles |
| markers per chunk | 0 |
| subdivision modelled | {16,1,16} |
| composited settlement | Lumbridge Settlement X3 |
| render triangles | 29242 |
| collision polys / vertices | 27413 / 14864 |

The two collision counts are also declared in `terrain_f2p_settlement_sealed_scene_col.c` as `scene_polygons[N]` and
`scene_vertices[N]`, so a scene whose provenance file has gone missing can still be identified:
dry-run the emitter at each candidate setting and match those two numbers.
